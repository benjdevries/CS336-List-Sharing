import { VerifyEmailDirective } from './verify-email.directive';

describe('VerifyEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyEmailDirective();
    expect(directive).toBeTruthy();
  });
});
